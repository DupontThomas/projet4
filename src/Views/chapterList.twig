{% extends "layout.twig" %}

        {% block title %}Liste des chapitres{% endblock %}


        {% block contents %}
            {% for content in contents %}

            <section>
                <h3><strong>{{ content.title }}</strong> | Publié le {{ content.postDate }}</h3>
                <div>
                    <div class="extract">{{ content.content | raw }}</div>
                    <a href="http://localhost/projet4/public/?page=chapter&id={{ content.id }}">Lire la suite</a>
                </div>

            </section>

            {% endfor %}
        {% endblock contents %}